//import { shallow } from 'enzyme';
//import { RecipeDetail } from '../components/RecipeDetail';

var enzyme = require('enzyme');
var assert = require('assert');

describe('RecipeTest', function(){
    
    describe('Create', function(){
        it('The button "CREAR NUEVA RECETA" must open a create a modal (render a <RecipeDetail/>)', function(){
            //write test
            // const wrapper = enzyme.shallow(<RecipeDetail/>);
            // expect(wrapper.find("Crear Receta"));
        });
    });

    describe('Create', function(){
        it('The information submited by the user is not corrupt', function(){
            //write the test
        });
    });

    describe('Create', function(){
        it('The new element should appear in the list of recepies', function(){
            //write the test
        });
    });

});

export{}
