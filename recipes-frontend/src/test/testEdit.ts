//import { shallow } from 'enzyme';
//import { RecipeDetail } from '../components/RecipeDetail';

import { ChangeCircleRounded } from "@mui/icons-material";

var enzyme = require('enzyme');
var assert = require('assert');

describe('RecipeTest', function(){
    
    describe('Edit', function(){
        it('The button "EDITAR" must open an update modal (render a <RecipeDetail/>)', function(){
            //write test
            // const wrapper = enzyme.shallow(<RecipeDetail/>);
            // expect(wrapper.find("Crear Receta"));
        });
    });

    describe('Edit', function(){
        it('The information submited by the user in the update form is not corrupt', function(){
            //write the test
        });
    });

    describe('Edit', function(){
        it('The number of recipes remain the same after the update', function(){
            //write the test
        });
    });

    describe('Edit', function(){
        it("if the recipe's name is Changed, this change must appear in the table", function(){
            //write the test
        });
    });
});

export{}
